
// ┌─────────────────────────────────────────────────────────────────┐
// │                    USER INTERACTION FLOW                         │
// ├─────────────────────────────────────────────────────────────────┤
// │                                                                   │
// │   👤 User Types in Input                                          │
// │            ↓                                                      │
// │   📝 setMessage() runs                                            │
// │            ↓                                                      │
// │   🔄 React re-renders component                                   │
// │            ↓                                                      │
// │   📦 All local variables reset                                    │
// │            ↓                                                      │
// │   ⏰ Old timer reference disappears ❌                            │
// │                                                                   │
// └─────────────────────────────────────────────────────────────────┘

// ┌─────────────────────────────────────────────────────────────────┐
// │                    VARIABLE BEHAVIOR COMPARISON                   │
// ├─────────────────────────────────────────────────────────────────┤
// │                                                                   │
// │  ┌─────────────────────┬─────────────────┬───────────────┐      │
// │  │      Variable       │ Survives Render │ Triggers      │      │
// │  │        Type         │                 │ Re-render     │      │
// │  ├─────────────────────┼─────────────────┼───────────────┤      │
// │  │ 📦 Normal Variable  │       ❌        │      ❌       │      │
// │  │ 📌 useState         │       ✅        │      ✅       │      │
// │  │ 🔗 useRef           │       ✅        │      ❌       │      │
// │  └─────────────────────┴─────────────────┴───────────────┘      │
// │                                                                   │
// └─────────────────────────────────────────────────────────────────┘

// ┌─────────────────────────────────────────────────────────────────┐
// │                    WHY useRef WORKS                               │
// ├─────────────────────────────────────────────────────────────────┤
// │                                                                   │
// │   📝 CODE:                                                        │
// │   const typingTimeoutRef = useRef(null);                         │
// │                                                                   │
// │   🧠 WHAT REACT CREATES:                                          │
// │   {                                                               │
// │     current: null  ← This object persists!                       │
// │   }                                                               │
// │                                                                   │
// │   ✨ PROPERTIES:                                                  │
// │   ✅ Survives all re-renders                                      │
// │   ✅ Keeps same memory reference                                  │
// │   ✅ NO re-render when changed                                    │
// │                                                                   │
// └─────────────────────────────────────────────────────────────────┘

// ┌─────────────────────────────────────────────────────────────────┐
// │                    VISUAL MEMORY ANALOGY                          │
// ├─────────────────────────────────────────────────────────────────┤
// │                                                                   │
// │   📦 NORMAL VARIABLE:                                            │
// │   Render 1  → ┌─────┐                                            │
// │              │ Box │                                            │
// │              └─────┘                                            │
// │                                                                   │
// │   Render 2  → Box DESTROYED ❌                                   │
// │              ┌─────┐                                            │
// │              │NewBox│  ← Different box                          │
// │              └─────┘                                            │
// │                                                                   │
// │   🔗 useRef:                                                     │
// │   Render 1  → ┌─────┐                                            │
// │              │ Box │                                            │
// │              └─────┘                                            │
// │                                                                   │
// │   Render 2  → ┌─────┐                                            │
// │              │ Box │  ← SAME box reused! ✅                     │
// │              └─────┘                                            │
// │                                                                   │
// └─────────────────────────────────────────────────────────────────┘

// ┌─────────────────────────────────────────────────────────────────┐
// │              WHY NOT useState FOR TIMERS?                         │
// ├─────────────────────────────────────────────────────────────────┤
// │                                                                   │
// │   const [timeoutId, setTimeoutId] = useState(null);              │
// │                                                                   │
// │   ⚠️ PROBLEMS:                                                    │
// │   ❌ Setting state triggers re-render                             │
// │   ❌ Timer ID is NOT visual data                                  │
// │   ❌ Unnecessary performance cost                                 │
// │                                                                   │
// │   ✅ BETTER WITH useRef:                                          │
// │   ✓ Store timers                                                  │
// │   ✓ Store DOM elements                                            │
// │   ✓ Store previous values                                         │
// │   ✓ Any mutable non-UI data                                       │
// │                                                                   │
// └─────────────────────────────────────────────────────────────────┘

// ┌─────────────────────────────────────────────────────────────────┐
// │                    QUICK REVISION CHECKLIST                       │
// ├─────────────────────────────────────────────────────────────────┤
// │                                                                   │
// │  ❓ When to use what?                                             │
// │                                                                   │
// │  🎯 useRef WHEN:                                                 │
// │  □ Timer IDs                                                      │
// │  □ DOM element references                                         │
// │  □ Previous values                                                │
// │  □ Any data that persists but doesn't affect UI                   │
// │                                                                   │
// │  🎯 useState WHEN:                                                │
// │  □ Data that displays in UI                                       │
// │  □ Values that should trigger re-render                          │
// │  □ Form inputs                                                    │
// │  □ Toggle states                                                  │
// │                                                                   │
// │  🎯 Normal Variables WHEN:                                        │
// │  □ Temporary calculations                                         │
// │  □ Loop counters                                                  │
// │  □ Data that can reset on re-render                               │
// │                                                                   │
// └─────────────────────────────────────────────────────────────────┘

// ┌─────────────────────────────────────────────────────────────────┐
// │                    DEBOUNCE EXAMPLE                               │
// ├─────────────────────────────────────────────────────────────────┤
// │                                                                   │
// │  const typingTimeoutRef = useRef(null);                          │
// │                                                                   │
// │  const handleChange = (e) => {                                   │
// │    setMessage(e.target value);                                   │
// │                                                                   │
// │    // ✅ Clear previous timer (always works!)                     │
// │    if (typingTimeoutRef.current) {                               │
// │      clearTimeout(typingTimeoutRef.current);                     │
// │    }                                                             │
// │                                                                   │
// │    // ✅ Set new timer (ref persists!)                            │
// │    typingTimeoutRef.current = setTimeout(() => {                 │
// │      console.log('Searching:', e.target value);                  │
// │    }, 500);                                                      │
// │  };                                                              │
// │                                                                   │
// └─────────────────────────────────────────────────────────────────┘

// ┌─────────────────────────────────────────────────────────────────┐
// │                    KEY TAKEAWAYS                                  │
// ├─────────────────────────────────────────────────────────────────┤
// │                                                                   │
// │  1️⃣  Re-renders reset normal variables ❌                        │
// │  2️⃣  useRef creates a STABLE object that survives renders ✅    │
// │  3️⃣  Updating useRef.current does NOT trigger re-render ⚡      │
// │  4️⃣  Perfect for timers, DOM refs, and non-UI data 🎯           │
// │  5️⃣  Think of useRef as a "secret box" that React remembers 📦  │
// │                                                                   │
// └─────────────────


///////////////////////////////////////////////////////////////////
// ┌─────────────────────────────────────────────────────────────────┐
// │                    COMPONENT LIFECYCLE FLOW                       │
// ├─────────────────────────────────────────────────────────────────┤
// │                                                                   │
// │   🏁 COMPONENT MOUNTS                                             │
// │            ↓                                                      │
// │   🎨 React renders UI for first time                              │
// │            ↓                                                      │
// │   ⚡ useEffect RUNS (with [] )                                    │
// │            ↓                                                      │
// │   🔌 Listeners/Subscriptions attached                             │
// │            ↓                                                      │
// │   🔄 State changes happen...                                      │
// │            ↓                                                      │
// │   🎨 React re-renders                                              │
// │            ↓                                                      │
// │   ❌ useEffect DOES NOT run again                                 │
// │            ↓                                                      │
// │   🗑️ COMPONENT UNMOUNTS                                           │
// │            ↓                                                      │
// │   🧹 Cleanup function runs (if provided)                          │
// │                                                                   │
// └─────────────────────────────────────────────────────────────────┘

// ┌─────────────────────────────────────────────────────────────────┐
// │                    DEPENDENCY ARRAY BEHAVIOR                      │
// ├─────────────────────────────────────────────────────────────────┤
// │                                                                   │
// │  ┌─────────────────┬─────────────────────────────────────────┐   │
// │  │   Syntax        │              When it runs               │   │
// │  ├─────────────────┼─────────────────────────────────────────┤   │
// │  │ useEffect(...)  │ After EVERY render (mount + updates)    │   │
// │  │ (no array)      │                                         │   │
// │  ├─────────────────┼─────────────────────────────────────────┤   │
// │  │ useEffect(...,  │ After FIRST render ONLY                  │   │
// │  │ [])             │ NEVER again                              │   │
// │  ├─────────────────┼─────────────────────────────────────────┤   │
// │  │ useEffect(...,  │ After FIRST render                       │   │
// │  │ [dep1, dep2])   │ AND when dep1 or dep2 change            │   │
// │  └─────────────────┴─────────────────────────────────────────┘   │
// │                                                                   │
// └─────────────────────────────────────────────────────────────────┘

// ┌─────────────────────────────────────────────────────────────────┐
// │                    WHAT [] MEANS                                  │
// ├─────────────────────────────────────────────────────────────────┤
// │                                                                   │
// │   ⚠️  WARNING: Technical explanation ahead!                      │
// │                                                                   │
// │   🔍 REACT ASKS:                                                  │
// │   "Does this effect depend on any props or state?"               │
// │                                                                   │
// │   📋 YOUR ANSWER (via []):                                        │
// │   "Nope! I don't depend on ANY values from the render."          │
// │                                                                   │
// │   🧠 REACT'S DECISION:                                            │
// │   "Okay, since you depend on nothing that can change,            │
// │    I'll run you ONCE after mount and NEVER again!"               │
// │                                                                   │
// │   💡 TRANSLATION:                                                 │
// │   [] = "Run me once, I'm independent"                            │
// │                                                                   │
// └─────────────────────────────────────────────────────────────────┘

// ┌─────────────────────────────────────────────────────────────────┐
// │                    VISUAL TIMELINE                                │
// ├─────────────────────────────────────────────────────────────────┤
// │                                                                   │
// │   Time ──────────────────────────────────────────────────→       │
// │                                                                   │
// │   [Mount]    [Render]    [Update]    [Render]    [Unmount]       │
// │     │           │           │           │           │            │
// │     ▼           ▼           ▼           ▼           ▼            │
// │                                                                   │
// │   useEffect    UI         State       UI         cleanup         │
// │   with []     shows      changes     updates     runs            │
// │     │                                           (if any)          │
// │     ▼                                                             │
// │   Attach                                                          │
// │   listeners                                                       │
// │     │                                                             │
// │     ▼                                                             │
// │   ⭐ ONLY RUNS HERE! ⭐                                           │
// │                                                                   │
// └─────────────────────────────────────────────────────────────────┘

// ┌─────────────────────────────────────────────────────────────────┐
// │                    COMMON USE CASES FOR []                        │
// ├─────────────────────────────────────────────────────────────────┤
// │                                                                   │
// │   ✅ PERFECT FOR:                                                │
// │                                                                   │
// │   1️⃣  API CALLS ON MOUNT:                                        │
// │      useEffect(() => {                                           │
// │        fetchUsers();  // Get data once                           │
// │      }, []);                                                     │
// │                                                                   │
// │   2️⃣  EVENT LISTENERS:                                           │
// │      useEffect(() => {                                           │
// │        window.addEventListener('resize', handleResize);          │
// │        return () => window.removeEventListener(...);             │
// │      }, []);  // Attach once, cleanup on unmount                 │
// │                                                                   │
// │   3️⃣  WEBSOCKET CONNECTION:                                      │
// │      useEffect(() => {                                           │
// │        const ws = new WebSocket('url');                          │
// │        return () => ws.close();                                  │
// │      }, []);  // Connect once                                    │
// │                                                                   │
// │   4️⃣  TIMERS/INTERVALS:                                          │
// │      useEffect(() => {                                           │
// │        const timer = setInterval(tick, 1000);                    │
// │        return () => clearInterval(timer);                        │
// │      }, []);  // Start once                                      │
// │                                                                   │
// └─────────────────────────────────────────────────────────────────┘

// ┌─────────────────────────────────────────────────────────────────┐
// │                    COMMON MISTAKES                                 │
// ├─────────────────────────────────────────────────────────────────┤
// │                                                                   │
// │   ❌ WRONG ASSUMPTIONS:                                           │
// │                                                                   │
// │   "My effect will run whenever I want it to"                     │
// │   → Nope! With [], it's ONE-TIME ONLY!                           │
// │                                                                   │
// │   "I put [] but my effect uses props"                            │
// │   → 🚨 DANGER! Stale closures ahead!                             │
// │                                                                   │
// │   "I can update state in [] effect freely"                       │
// │   → Yes, but it won't re-run based on that state!                │
// │                                                                   │
// │   ✅ REMEMBER:                                                    │
// │   useEffect with [] = "Run once, capture values at mount time"   │
// │                                                                   │
// └─────────────────────────────────────────────────────────────────┘

// ┌─────────────────────────────────────────────────────────────────┐
// │                    STALE CLOSURE WARNING ⚠️                       │
// ├─────────────────────────────────────────────────────────────────┤
// │                                                                   │
// │   🧟 THE PROBLEM:                                                 │
// │                                                                   │
// │   useEffect(() => {                                              │
// │     const timer = setInterval(() => {                            │
// │       console.log(count);  // count is from FIRST render!        │
// │     }, 1000);                                                    │
// │   }, []);  // [] captures count's INITIAL value                  │
// │                                                                   │
// │   🔧 THE FIXES:                                                   │
// │                                                                   │
// │   1. Add count to dependencies: [count]                          │
// │   2. Use useRef to store latest value                            │
// │   3. Use functional updates                                      │
// │                                                                   │
// └─────────────────────────────────────────────────────────────────┘

// ┌─────────────────────────────────────────────────────────────────┐
// │                    QUICK REVISION CHECKLIST                       │
// ├─────────────────────────────────────────────────────────────────┤
// │                                                                   │
// │   🎯 USE [] WHEN YOU NEED:                                       │
// │   □ One-time setup code                                           │
// │   □ Initial data fetch                                            │
// │   □ Subscribe to external services                                │
// │   □ Add global event listeners                                    │
// │   □ Start timers/intervals                                        │
// │                                                                   │
// │   🧠 REMEMBER:                                                    │
// │   □ [] = Empty = No dependencies                                 │
// │   □ Runs AFTER first render                                       │
// │   □ NEVER runs again (unless component remounts)                 │
// │   □ Cleanup runs on unmount                                       │
// │   □ Watch out for stale closures!                                 │
// │                                                                   │
// └─────────────────────────────────────────────────────────────────┘

// ┌─────────────────────────────────────────────────────────────────┐
// │                    VISUAL ANALOGY                                 │
// ├─────────────────────────────────────────────────────────────────┤
// │                                                                   │
// │   🏠 MOVING INTO A NEW APARTMENT:                                 │
// │                                                                   │
// │   useEffect with [] =                                             │
// │                                                                   │
// │   Day 1 (Mount):                                                  │
// │   - Hang curtains ✅                                              │
// │   - Set up WiFi ✅                                                │
// │   - Install locks ✅                                              │
// │                                                                   │
// │   Day 2 (State change):                                           │
// │   - Buy new furniture                                             │
// │   - ❌ Don't re-hang curtains!                                    │
// │   - ❌ Don't re-setup WiFi!                                       │
// │                                                                   │
// │   Moving out (Unmount):                                           │
// │   - Take down curtains                                            │
// │   - Cancel WiFi                                                   │
// │                                                                   │
// └─────────────────────────────────────────────────────────────────┘

// ┌─────────────────────────────────────────────────────────────────┐
// │                    ONE-LINER SUMMARY                              │
// ├─────────────────────────────────────────────────────────────────┤
// │                                                                   │
// │   📌 useEffect with [] =                                          │
// │   "Run this ONCE after mount and forget about it"                 │
// │                                                                   │
// │   Think of it as:                                                 │
// │   componentDidMount + componentWillUnmount                        │
// │   (but not componentDidUpdate)                                    │
// │                                                                   │
// └─